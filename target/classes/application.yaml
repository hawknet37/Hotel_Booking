server:
  port: 8080
  servlet:
    context-path: "/api/v1"

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/HotelBooking"
    username: root
    password: thiennguyen

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true